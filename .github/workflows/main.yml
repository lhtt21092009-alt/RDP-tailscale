name: RDP-Tailscale-Chrome-Only

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: 1. Cấu hình Windows & User
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP" dir=in action=allow protocol=TCP localport=3389
          
          $username = "RDP"
          $password = "Oibanoi874@" 
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          if (Get-LocalUser -Name $username -ErrorAction SilentlyContinue) { Remove-LocalUser -Name $username }
          New-LocalUser -Name $username -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member $username
          Restart-Service -Name TermService -Force

      - name: 2. Cài đặt & Kết nối Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath -UseBasicParsing
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-game-runner
          
          $tsIP = ""
          for ($i=0; $i -lt 12; $i++) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              if ($tsIP) { break }
              Start-Sleep -Seconds 5
          }
          echo "TS_IP=$tsIP" >> $env:GITHUB_ENV

      - name: 3. Cài đặt Chrome ra Desktop
        run: |
          $desktopPath = "C:\Users\RDP\Desktop"
          if (!(Test-Path $desktopPath)) { New-Item -ItemType Directory -Path $desktopPath -Force }
          
          # Phân quyền chuẩn để không bị lỗi term 'OI'
          icacls $desktopPath /grant "RDP:(OI)(CI)F" /T
          
          Write-Host "Dang tai bo cai Chrome..."
          $chromeUrl = "https://dl.google.com/chrome/install/375.126/chrome_installer.exe"
          Invoke-WebRequest -Uri $chromeUrl -OutFile "$desktopPath\Setup_Chrome.exe" -UseBasicParsing
          
          Write-Host "Chrome setup da co san ngoai Desktop."

      - name: 4. THÔNG TIN KẾT NỐI (HIỆN RÕ)
        run: |
          Write-Host "`n"
          Write-Host "############################################################" -ForegroundColor Cyan
          Write-Host "      THONG TIN DANG NHAP REMOTE DESKTOP (RDP)" -ForegroundColor Yellow
          Write-Host "############################################################" -ForegroundColor Cyan
          Write-Host " [>] IP ADDRESS : $env:TS_IP" -ForegroundColor Green
          Write-Host " [>] USERNAME   : RDP" -ForegroundColor Green
          Write-Host " [>] PASSWORD   : Oibanoi874@" -ForegroundColor Green
          Write-Host "############################################################" -ForegroundColor Cyan
          Write-Host " Huong dan: Mo Tailscale tren may ban truoc khi ket noi!"
          Write-Host "############################################################`n"
          
          # Mở trình duyệt mặc định sẵn
          Start-Process "https://www.google.com" 
          
          while ($true) {
              $time = Get-Date -Format "HH:mm:ss"
              Write-Host "[$time] Runner dang online tai IP: $env:TS_IP"
              Start-Sleep -Seconds 300
          }
